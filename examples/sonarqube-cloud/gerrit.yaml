---
# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: 2026 The Linux Foundation

# =============================================================================
# EXAMPLE: Gerrit-wrapped caller for the SonarQube Cloud lane
# =============================================================================
# Use this form when Gerrit is the source of truth (SCM) for your project.
# gerrit_to_platform dispatches this workflow (via the GitHub API) when a
# patchset is created in Gerrit; it then votes Verified +/-1 on the change.
#
# DEPLOYMENT NOTES:
#   * Copy into .github/workflows/ with a filename containing BOTH 'gerrit'
#     AND the lane name so gerrit_to_platform can discover it, e.g.
#     gerrit-sonar.yaml.
#   * The workflow file must exist on EVERY active branch: dispatches
#     target refs/heads/{GERRIT_BRANCH}.
#   * The nine GERRIT_* inputs are populated automatically by the dispatcher.
#   * GERRIT_DISABLED lets you run manually from the GitHub UI without Gerrit
#     (the clear/vote jobs are skipped and a normal checkout is used).
#   * Set the repository variables GERRIT_SERVER, GERRIT_SSH_USER,
#     GERRIT_KNOWN_HOSTS, GERRIT_URL, and the secrets GERRIT_SSH_PRIVKEY
#     and SONAR_TOKEN.
#   * The Maven pre-scan build shown below additionally needs the
#     variable NEXUS_USERNAME and the secret NEXUS_PASSWORD, which drive
#     settings.xml synthesis. Neither is needed for build_type: none, or
#     where the build needs no credentials; drop the maven_* inputs and
#     maven_server_password with them.
#   * Gerrit voting connects over SSH (tcp/29418). Ensure the harden-runner
#     allow-list in use carries that entry, or the vote job fails in block
#     mode.
#
# BRANCH DECORATION:
#   Sonar infers branch context from the CI provider, and under Gerrit
#   that inference is wrong: the dispatch looks like a workflow_dispatch
#   on the target branch, so every patchset overwrites the branch's
#   analysis. Set sonar_branch_name to keep per-change results separate.
#   Note that each distinct name creates a branch in the Sonar project,
#   which consumes quota on some plans, so scan the branch rather than
#   the change unless per-change results are genuinely needed.
# =============================================================================
name: 'Gerrit SonarQube Cloud'

# yamllint disable-line rule:truthy
on:
  workflow_dispatch:
    inputs:
      GERRIT_BRANCH:
        description: 'Branch that change is against'
        required: false
        type: string
        default: ''
      GERRIT_CHANGE_ID:
        description: 'The ID for the change'
        required: false
        type: string
        default: ''
      GERRIT_CHANGE_NUMBER:
        description: 'The Gerrit number'
        required: false
        type: string
        default: ''
      GERRIT_CHANGE_URL:
        description: 'URL to the change'
        required: false
        type: string
        default: ''
      GERRIT_EVENT_TYPE:
        description: 'Type of Gerrit event'
        required: false
        type: string
        default: ''
      GERRIT_PATCHSET_NUMBER:
        description: 'The patch number for the change'
        required: false
        type: string
        default: ''
      GERRIT_PATCHSET_REVISION:
        description: 'The revision sha'
        required: false
        type: string
        default: ''
      GERRIT_PROJECT:
        description: 'Project in Gerrit'
        required: false
        type: string
        default: ''
      GERRIT_REFSPEC:
        description: 'Gerrit refspec of change'
        required: false
        type: string
        default: ''
      GERRIT_DISABLED:
        description: 'Run without Gerrit components'
        required: false
        default: false
        type: boolean

concurrency:
  group: ${{ github.event.inputs.GERRIT_CHANGE_ID || github.run_id }}
  cancel-in-progress: true

permissions: {}

jobs:
  # Clear any previous Verified vote so the change starts from a clean state.
  clear-vote:
    if: inputs.GERRIT_DISABLED != true
    runs-on: ubuntu-latest
    permissions:
      contents: read
    timeout-minutes: 5
    steps:
      - name: 'Harden runner'
        # yamllint disable-line rule:line-length
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920  # v2.20.0
        with:
          egress-policy: audit
      - name: 'Clear votes'
        # yamllint disable-line rule:line-length
        uses: lfreleng-actions/gerrit-review-action@a1c036a59b94c8ab89de26d378f6a28cc7378b13  # v1.1.2
        with:
          host: ${{ vars.GERRIT_SERVER }}
          username: ${{ vars.GERRIT_SSH_USER }}
          key: ${{ secrets.GERRIT_SSH_PRIVKEY }}
          known_hosts: ${{ vars.GERRIT_KNOWN_HOSTS }}
          gerrit-change-number: ${{ inputs.GERRIT_CHANGE_NUMBER }}
          gerrit-patchset-number: ${{ inputs.GERRIT_PATCHSET_NUMBER }}
          vote-type: clear
      - name: 'Allow replication'
        run: sleep 10s

  # Call the SonarQube Cloud lane. When dispatched by Gerrit, the gerrit_*
  # inputs drive checkout-gerrit-change-action inside the lane. For a manual
  # GERRIT_DISABLED run the GERRIT_* inputs are empty, so the lane performs
  # a normal checkout.
  sonarqube-cloud:
    needs: clear-vote
    if: >-
      always()
      && (needs.clear-vote.result == 'success'
          || needs.clear-vote.result == 'skipped')
    permissions:
      contents: read
    # REPLACE the pinned SHA below with a security-workflows release SHA.
    # The all-zero SHA is a non-functional placeholder, NOT a real commit.
    # yamllint disable-line rule:line-length
    uses: lfreleng-actions/security-workflows/.github/workflows/sonarqube-cloud.yaml@0000000000000000000000000000000000000000  # REPLACE: vX.Y.Z
    with:
      # When GERRIT_DISABLED is true the gerrit_* values are blanked so the
      # lane falls back to a standard checkout.
      # yamllint disable-line rule:line-length
      gerrit_refspec: ${{ !inputs.GERRIT_DISABLED && inputs.GERRIT_REFSPEC || '' }}
      # yamllint disable-line rule:line-length
      gerrit_project: ${{ !inputs.GERRIT_DISABLED && inputs.GERRIT_PROJECT || '' }}
      # yamllint disable-line rule:line-length
      gerrit_branch: ${{ !inputs.GERRIT_DISABLED && inputs.GERRIT_BRANCH || '' }}
      # gerrit_url falls back to the GERRIT_URL repository variable.
      #
      # Analyse the Gerrit target branch rather than letting Sonar infer
      # branch context from the dispatch. Swap for a per-change name such
      # as 'change-${{ inputs.GERRIT_CHANGE_NUMBER }}' where per-patchset
      # results are needed, and read the note at the top of this file.
      sonar_branch_name: ${{ inputs.GERRIT_BRANCH }}
      #
      # Build the project so the Java analyser sees bytecode and a JaCoCo
      # coverage report. Leave java_version empty to auto-detect the
      # project's JDK.
      #
      # analysis_mode is left unset, so the lane derives it from
      # build_type and a Maven project is analysed with
      # sonar-maven-plugin. That matters: the Scanner CLI would also walk
      # target/generated-sources and report findings against generated
      # code. Set analysis_mode explicitly only to override.
      build_type: 'maven'
      mvn_params: '--fail-at-end'
      # Dependency fetches from artifact CDNs are impractical to enumerate
      # in the egress allow-list; audit the job rather than blocking it.
      build_permit_egress_traffic: true
      #
      # Describe the Maven servers so the lane synthesises settings.xml.
      # The values below are the ONAP shape: five server ids sharing one
      # credential, the onap-public mirror, and the onap-nexus profile.
      # yamllint disable-line rule:line-length
      maven_server_ids: 'ecomp-releases,ecomp-snapshots,onap-releases,onap-snapshots,nexus3.onap.org:10003'
      maven_server_username: ${{ vars.NEXUS_USERNAME }}
      maven_mirror_id: 'onap-public'
      maven_mirror_url: 'https://nexus.onap.org/content/groups/public/'
      maven_profile_id: 'onap-nexus'
      # yamllint disable-line rule:line-length
      maven_profile_repository_url: 'https://nexus.onap.org/content/groups/public/'
    secrets:
      sonar_token: ${{ secrets.SONAR_TOKEN }}
      maven_server_password: ${{ secrets.NEXUS_PASSWORD }}
      # Alternatively, supply a complete settings.xml instead of the
      # maven_* inputs above:
      # maven_global_settings: ${{ secrets.MAVEN_GLOBAL_SETTINGS }}

  # Report the overall workflow conclusion back to Gerrit as a Verified vote.
  vote:
    if: ${{ always() && inputs.GERRIT_DISABLED != true }}
    needs: [clear-vote, sonarqube-cloud]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      # workflow-conclusion queries the Actions API to list run jobs
      actions: read
    timeout-minutes: 5
    steps:
      - name: 'Harden runner'
        # yamllint disable-line rule:line-length
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920  # v2.20.0
        with:
          egress-policy: audit
      - name: 'Get conclusion'
        # yamllint disable-line rule:line-length
        uses: im-open/workflow-conclusion@7b146947e59eaf7fc3328822357861ab1758c088  # v2.2.5
      - name: 'Set vote'
        # yamllint disable-line rule:line-length
        uses: lfreleng-actions/gerrit-review-action@a1c036a59b94c8ab89de26d378f6a28cc7378b13  # v1.1.2
        with:
          host: ${{ vars.GERRIT_SERVER }}
          username: ${{ vars.GERRIT_SSH_USER }}
          key: ${{ secrets.GERRIT_SSH_PRIVKEY }}
          known_hosts: ${{ vars.GERRIT_KNOWN_HOSTS }}
          gerrit-change-number: ${{ inputs.GERRIT_CHANGE_NUMBER }}
          gerrit-patchset-number: ${{ inputs.GERRIT_PATCHSET_NUMBER }}
          vote-type: ${{ env.WORKFLOW_CONCLUSION }}
